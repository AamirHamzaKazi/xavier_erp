import 'package:flutter/material.dart';

// Teacher theme colors
const kTeacherPrimaryColor = Color(0xFF9747FF);
const kTeacherBackgroundColor = Color(0xFF1A1A1A);

// Student theme colors
const kStudentPrimaryColor = Color(0xFF40E0FD);
const kStudentBackgroundColor = Color(0xFF1A1A1A);

// Admin theme colors
const kAdminPrimaryColor = Color(0xFF00FF00);  // Neon green color
const kAdminBackgroundColor = Color(0xFF1A1A1A);

// Common colors
const kBackgroundColor = Color(0xFF1A1A1A); 